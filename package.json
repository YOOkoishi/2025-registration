{
  "name": "2025-registration",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "gen": "node scripts/build-data.js",
    "dev": "npm run gen && vite",
    "build": "npm run gen && vite build --base=/2025-registration/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.2.4"
  },
  "dependencies": {
    "fs-extra": "^11.3.2",
    "matter-js": "^0.20.0"
  }
}
